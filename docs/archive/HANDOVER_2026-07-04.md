# 引き継ぎドキュメント（2026-07-04時点）

**目的**: 次の会話セッションのClaudeが、このドキュメントだけで作業を再開できるようにする。
**リポジトリ**: https://github.com/samkaz15/fortune （このリポジトリが唯一の正）
**応対言語**: 日本語。技術判断は率直に。過剰な承認・忖度はしない。

---

## 1. プロジェクト概要

AI占いサービス「**糸町の少年**」。TikTok/Instagram流入を想定。
コンセプトは当初「未来を当てる占い」だったが、CEO_UPDATE（2026-07-03）で
**「今日の意思決定を最適化する行動支援サービス」へ転換済み**。

体制: CEO（ユーザー本人）＋ Claude（設計/実装）＋ ChatGPT（企画/コピー・別セッションでシミュレート）
＋ Gemini（調査・同）＋ Sakana AI（占術統合・API連携予定）。
**別のClaudeセッションが並行稼働することがある**（過去にツクヨミ前提のGM9/GPT8コミットが入り整合修正した実績あり）。
**push前に必ず `git fetch origin` で差分確認すること。**

## 2. 確定済みのCEO意思決定（docs/design/00_ceo_decisions/ 参照）

- **キャラクター**: 「糸町の少年」本人。カエル／男の子／一人称「僕」／普通の話し方／常にポジティブ・決め打ち。
  ツクヨミ案は不採用。タイミングの迷い表現・ネガティブな含みは禁止（crisis対応のみ例外）。
- **占術の内訳は非開示**（「糸町の少年スタイル」として扱う）。
- **CEO1**: 相談チャットはカテゴリ別占術固定
  （BUSINESS→算命学 / SELF,TODAY→ホロスコープ+四柱推命 / RELATIONSHIP,COMPATIBILITY→姓名判断）。
  ※各占術の計算精度（画数辞書・節入り暦計算・命式）は暫定＝**次回CEO監修事項**。
- **CEO2**: 面談チケット＝公式LINE電話・1時間・CEO本人対応。入札は現在最高額+100円以上。
- **CEO_UPDATE（意思決定レポート）**: 日次レポート6項目固定
  （①★+100点 ②キーワード3つ ③総合要約150-250字 ④注意3項目 ⑤統合アドバイス ⑥今日やるべき行動1つ）。
  気象用語禁止（人間行動キーワードへ翻訳）。占術役割固定（四柱推命=タイミング/算命学=仕事/
  ホロスコープ=心理/姓名判断=人間関係）。スコアはルールベース確定・AIは変更しない。
  設計書: `docs/design/05_decision_report/production_design.md`
- **CL18（神社）**: CEOが週1回以上継続更新する運用タスク（WBS上「継続運用中」＝青）。
- **CL22（LINE）**: 外部公式LINEへのhrefリンク誘導のみ。URL: **https://lin.ee/yJUpS8g**
  （`NEXT_PUBLIC_LINE_OFFICIAL_URL` に設定済み）。
- **Stripe公開キー（LIVE）**: pk_live_51TpHeq33BrlNeoXuYitxzoRH1ma6470vUUlprlDgtMroBRDjmirtFQacWE03ZiXrzyxkEFl2jaqg09IQsjZEb1Kr00COV38oyx
  ※Checkoutリダイレクト方式のため現実装では実質未使用。テストキー発行を推奨済み（回答待ち）。

## 3. 技術スタック（確定・変更しないこと）

Next.js 14.2.35 (App Router) + TypeScript + Tailwind /
**Prisma 7 エンジンレス + @prisma/adapter-pg**（generator: `provider="prisma-client"`,
output=`../src/generated/prisma`。接続URLは `prisma.config.ts` 経由。schema.prismaにurlを書かない）/
PostgreSQL / Redis（Upstash。未設定時はインメモリ自動フォールバック実装済み）/ Stripe Checkout / Zod。

### サンドボックス環境の再現手順（重要）
- Prismaのバイナリ取得は403で失敗する。回避策:
  `$HOME/.cache/prisma/master/3c6e192761c0362d496ed980de936e2f3cebcd3a/debian-openssl-3.0.x/schema-engine`
  に `exit 0` するダミースクリプトを置き、`PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 npx prisma generate`。
- マイグレーションは手書きDDL: `prisma/manual_init.sql` → `manual_phase2.sql` → `manual_decision_report.sql` を psql で適用。
- ローカルPostgres16（user=itomachi / pass=itomachi_dev / db=itomachi）。起動:
  `su postgres -c '/usr/lib/postgresql/16/bin/pg_ctl -D /var/lib/postgresql/16/main -o "-c config_file=/etc/postgresql/16/main/postgresql.conf" -l /tmp/pg.log start'`
- bash実行環境はコマンド間でプロセスが切れるため、**サーバー起動→テストは同一 `bash -c` 内で完結**させる。
- `next/font/google` はオフラインで失敗するため使用禁止（globals.cssの@import+フォールバック方式に変更済み）。

## 4. 実装状況（結合テスト70項目全パス。scripts/integration_test.sh）

- **Phase1（CL1-CL13）**: 認証(開発用Cookie仮実装)／占術エンジン4種(暫定ロジック)／
  占いチャット(1日5回制限・crisis検知)／決済3種(サブスク初月500円→980円、クレジット300円/5回、
  オークション楽観ロック100円刻み)／Stripe Webhook(冪等)／マイページ／天気連携。
- **Phase2（CL15-CL23）**: 相性診断相手情報UI／運気カレンダー(/calendar)／通知高度化
  (NotificationLog、/api/notifications/evaluate=CRON_SECRET保護)／神社(/shrines、GPT8の5ブロック)／
  推薦(recommendation.ts、k=20匿名性)／紹介制度(referralCode、/invite/[code]、双方1pt、
  消費順=無料枠→ポイント→クレジット)／ランキング(Redis15分キャッシュ)。
- **意思決定レポート**: 5層アーキテクチャ実装済み（src/lib/decision-report/:
  environment.ts=気圧→行動キーワード翻訳・LLMに気圧生値を渡さない / knowledge.ts=RAG構造化 /
  scoring.ts=決定論スコア+scoreBreakdown監査 / index.ts=Zod検証+1リトライ+フォールバック）。
  /api/report/today（1日1回・レース対策・クォータ非消費）、/report画面、TOPのCTAは/reportへ変更済み。
- プロンプト: `prompts/chat/system_prompt.v2.0.md`（v2.0が正。v1.0=ツクヨミは履歴）、
  `prompts/chat/decision_report_task.v1.0.md`、`prompts/content/next_action_templates.v2.0.json`。

## 5. WBS運用

- 最新ファイル: `/mnt/user-data/outputs/WBS_progress_latest.xlsx`
  （**日本語ファイル名はpresent_filesで文字化けするため英語名で出す**）。
- 生成スクリプト: `/home/claude/build_excel3_progress2.py`、データ: `/home/claude/wbs3_tasks.pkl`
  （サンドボックスはリセットされるため、次セッションで無ければWBSはリポジトリの状況から再構築）。
- 進捗: Phase1=30/33（残: CEO3・CL14。黄=CL8占術精度）／Phase2=10/14
  （青=CL18/CEO4継続運用。残: CEO5・CL24）／Phase3=0/11。
- ステータス色: 完了=緑 / 継続運用=青 / 一部完了(要見直し)=黄 / 未着手=担当AI色。

## 6. GitHub運用

- push方式: ユーザーがPATを都度発行 → `git push "https://TOKEN@github.com/samkaz15/fortune.git" main`。
  トークンはgit設定に保存しない（URL埋め込みの一回使用のみ）。使用後は失効を推奨。
- 最新push済みコミット: `00bf773`（CL13-CL23＋CL22簡略化＋意思決定レポート全反映）。
- **push前に必ず fetch して並行セッションの差分を確認**（過去に競合実績あり）。

## 7. 現在進行中: UIモックアップ／LP制作（CL5拡張）

### 確定済みUI要件
- 会員フロー: 無料 → 登録（**メールアドレス＋ユーザー名のみ**）→ メール認証（6桁コード）→ プラン選択・決済。
  ※実コードの登録はパスワード+姓名+生年月日必須のままなので、**この簡略フローを実コードに反映するか要判断**（未決）。
- フォント: `font-family: "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans", Meiryo, sans-serif;`
- 丸みを帯びたセクション。ボタンは立体感（下side厚み影＋上端ハイライト＋押下で沈む）。
- 配色は60:30:10。**ユーザーはP2（紺碧×緑）を選択済み**:
  ベース#F7F9FB / メイン#16334F / アクセント#17A85B（hover #0F8A49 / 押下 #0B6E3A）/ テキスト#2E3338。
  ダークモード: bg#0E161F / surface#182634 / text#E7ECF1（アクセントは継承）。

### 成果物（/mnt/user-data/outputs/）
- `itomachi_mockup_v1.html`: 8画面モック＋配色3パターン切替。
- `itomachi_mockup_v2.html`: **P2デフォルト＋ライト/ダークスイッチ付き（最新のアプリUIモック）**。
- `itomachi_lp_v1.html`: **chain.comスタイルのLP（最新・完成）**。P2ダーク配色。
  構成: 巨大タイポHero「大丈夫。必ずうまくいく！」＋公式カエル画像（base64埋込・浮遊アニメ）／
  マーキー帯／`_LABEL`形式マイクロラベル／6項目レポートのデモUI／番号付きカード／料金／FAQ／
  フッター大文字マーキー／スクロールリビール。
- 公式カエル画像の元データ: ユーザーアップロード
  `Gemini_Generated_Image_uf4lmpuf4lmpuf4l.png`（瞑想する青いカエル・鉢巻に「糸」）。
  ※サンドボックスリセット後は再アップロードを依頼すること。

### LPの次のステップ（未着手）
1. ユーザーのフィードバックを受けて改善イテレーション。
2. 確定後、LPをNext.js実装（トップの未ログイン向けページ or 独立LP）としてリポジトリに取り込む。

## 8. その他の未完了タスク

1. **インフラ構築（全部ユーザー作業・未着手）**: Vercel / Supabase / Upstash 各アカウント作成、
   Stripe商品作成（月980円Price＋初月500円Coupon）＋Webhook登録。
   → 「DEPLOYMENT.md を作って」と言われたら画面操作レベルの手順書を作る（提案済み・未作成）。
2. CEO3（Phase1レビュー）→ CL14（ソフトローンチ）→ CEO5 → CL24。Phase3（CL25-33）未着手。
3. 占術ロジック精度確定（画数辞書・節入り暦・命式）＝次回CEO監修待ち。
4. 技術TODO: プロフィール姓名分離保存（現状 `name.slice(0,1)`）／Supabase Auth移行／
   News・お知らせのモデル追加（現在ダミー）／モックの簡略登録フローの実コード反映判断。

## 9. 引き継ぎ時の注意

- 「先に動くものがある方を正とする」原則。ARCHITECTURE.md / PROJECT_CHARTER.md は
  Next.js実装準拠に書き換え済み（旧FastAPI/K8s案は不採用）。
- crisis検知（自傷等→固定文面で窓口案内・課金消費なし）は「常にポジティブ」原則の例外として必ず維持。
- ユーザーがトークンや秘密鍵をチャットに貼った場合: 公開キーはOK、シークレットキーは貼らないよう案内。
  使用済みトークンの失効を促す。
